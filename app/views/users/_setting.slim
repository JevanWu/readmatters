= simple_form_for @user, url: update_info_path, method: :patch, html: { class: 'form-horizontal' } do |f|
  = f.input :name
  = f.input :phone
  .form-group
    = f.submit t("save"), class: "btn btn-default"
